{{ elastic_log_rotate_path }}/*.log
{
        copytruncate
        daily
        rotate 2
        delaycompress
        compress
        dateext
        notifempty
        missingok
        maxage 5
}
