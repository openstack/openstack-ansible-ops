---
component_skel:
  cluster-metrics:
    belongs_to:
      - cluster-metrics_all

container_skel:
  cluster-metrics_container:
    belongs_to:
      - log_containers
    contains:
      - cluster-metrics
